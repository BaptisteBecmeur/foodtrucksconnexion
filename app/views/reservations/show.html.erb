      <% content_for(:after_js) do %>
        <%= javascript_tag do %>
        $(document).ready(function() {
        <!-- alert('<%= j @alert_message %>');
      }); -->
      var coord = <%= raw @foodtruck_coordinates %>;
      alert('Foodtruck coordinates are ' + coord.lat + ', ' + coord.lng);
      });
    <% end %>
